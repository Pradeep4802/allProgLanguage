#include<stdio.h>

int main() {
    FILE *ptr;
    // ptr = fopen("Sample2.txt","r"); //--> for reding the file
    // ptr = fopen("Sample2.txt","r"); //--> for reding the file
    ptr = fopen("Sample2.txt","w"); //--> for writing the file
    return 0;
}