#include<stdio.h>
#include<stdlib.h>

struct node
{
    int data;
    struct node *next;
};

int main()
{
    int n;
    struct node *head;
    head = (struct node *) malloc (sizeof(struct node));

    printf("\n Enter the number of the node: ");
    scanf("%d",&n);

    for (int i = 1; i <= n; i++)
    {
        printf
    }
    
}